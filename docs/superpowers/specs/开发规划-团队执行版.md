# agent_recheck 开发规划

**版本**：v1.2
**日期**：2026-04-12
**负责人**：AI 团队
**状态**：Phase 1 Week 4 完成

---

## 一、项目信息

| 项目 | 说明 |
|------|------|
| **名称** | agent_recheck |
| **类型** | Python CLI 工具 |
| **目标** | 政府采购招投标文件合规性审查智能体 |
| **技术栈** | Python 3.10+, qwen3.5-27b |
| **部署环境** | 隔离内网 |

---

## 二、开发环境准备

### 2.1 前置检查

```bash
# Python 版本
python3 --version  # 要求 >= 3.10

# 依赖检查
pip3 list | grep -E "docx|pdfplumber|typer|pydantic"

# LLM 服务检查
curl -X POST http://localhost:8000/v1/models  # 或配置的地址
```

### 2.2 项目初始化

```bash
# 创建项目目录
mkdir -p agent_recheck
cd agent_recheck

# 初始化 Python 项目
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# 安装基础依赖
pip install --upgrade pip
pip install python-docx pdfplumber pyyaml typer rich pydantic structlog httpx
```

---

## 三、模块开发规划

### Phase 0: 基础设施（2周）⭐ 最高优先级

#### Week 1: 项目脚手架 + 文档解析基础

| Day | 任务 | 交付物 | 状态 |
|-----|------|--------|------|
| 1 | 创建项目目录结构 | 项目骨架 | ✅ |
| 2 | 配置 pyproject.toml | 依赖管理 | ✅ |
| 3 | 配置日志系统 | utils/logging.py | ✅ |
| 4 | DOCX 解析器基础 | parser/docx_parser.py | ✅ |
| 5 | PDF 解析器基础 | parser/pdf_parser.py | ✅ |

#### Week 2: 表格解析增强（核心）

| Day | 任务 | 交付物 | 状态 |
|-----|------|--------|------|
| 6 | 表格结构识别 | parser/table_parser.py | ✅ |
| 7 | 表格标题关联 | parser/table_parser.py | ✅ |
| 8 | 深圳格式适配 | parser/shenzhen_adapter.py | ✅ |
| 9 | ★▲标记识别 | parser/shenzhen_adapter.py | ✅ |
| 10 | 单元测试 | tests/test_parser.py | 🔄 |

**Phase 0 验收标准**：
- ✅ 支持深圳 DOCX 文件解析
- ✅ 表格解析准确率 ≥ 80%
- ✅ 识别 ★ 和 ▲ 标记
- ✅ 单元测试覆盖率 ≥ 80%

---

### Phase 1: 核心能力（4周）

#### Week 3: 规则引擎

| Day | 任务 | 交付物 | 状态 |
|-----|------|--------|------|
| 11 | 规则数据结构定义 | models/rule.py | ✅ |
| 12 | 基础规则加载器 | engine/rule_loader.py | ✅ |
| 13 | 规则匹配引擎 | engine/matcher.py | ✅ |
| 14 | 12个核心规则实现 | rules/discrimination/*.yaml | ✅ |
| 15 | 规则自检工具 | tools/rule_validator.py | ⬜ |

#### Week 4: LLM 集成

| Day | 任务 | 交付物 | 状态 |
|-----|------|--------|------|
| 16 | LLM 客户端封装 | llm/client.py | ✅ |
| 17 | System Prompt 设计 | llm/prompts.py | ✅ |
| 18 | LLM 容错机制 | llm/fallback.py | ✅ |
| 19 | 分段审查策略 | llm/chunk_processor.py | 🔄 |
| 20 | Token 消耗追踪 | tracker/token_tracker.py | ✅ |

#### Week 5: 混合引擎 + 报告

| Day | 任务 | 交付物 |
|-----|------|--------|
| 21 | 混合分析引擎 | engine/hybrid_engine.py |
| 22 | 结果聚合器 | aggregator/merger.py |
| 23 | 风险去重逻辑 | aggregator/deduplicator.py |
| 24 | JSON 报告生成 | report/json_reporter.py |
| 25 | Markdown 报告 | report/md_reporter.py |

#### Week 6: CLI + 集成

| Day | 任务 | 交付物 |
|-----|------|--------|
| 26 | CLI 命令设计 | cli/main.py |
| 27 | 单文件分析命令 | cli/commands/analyze.py |
| 28 | 批量分析命令 | cli/commands/batch.py |
| 29 | 规则管理命令 | cli/commands/rules.py |
| 30 | 集成测试 | tests/test_integration.py |

**Phase 1 验收标准**：
- ✅ 支持 12 个核心规则
- ✅ LLM 接入正常
- ✅ 降级机制生效
- ✅ 支持 JSON/Markdown 报告
- ✅ CLI 命令可用

---

### Phase 2: 审查能力（3周）

#### Week 7-8: 规则扩展

| 任务 | 交付物 | 规则数量 |
|------|--------|---------|
| 非歧视性规则完善 | rules/discrimination/*.yaml | +10 |
| 采购需求规则 | rules/procurement/*.yaml | +8 |
| 评分标准规则 | rules/scoring/*.yaml | +5 |
| 合同条款规则 | rules/contract/*.yaml | +6 |
| 认证证书规则 | rules/certifications/*.yaml | +8 |

#### Week 9: 一致性检查

| 任务 | 交付物 |
|------|--------|
| 跨段落一致性检查 | consistency/paragraph_checker.py |
| 采购标的与标准一致性 | consistency/standard_checker.py |
| 法规引用有效性检查 | consistency/regulation_checker.py |
| 深圳知识库构建 | knowledge/regulations/shenzhen/ |

**Phase 2 验收标准**：
- ✅ 规则总数 ≥ 40 条
- ✅ 一致性检查模块完成
- ✅ 深圳知识库基础版本

---

### Phase 3: 准确率保障（2-3周）

| 任务 | 交付物 |
|------|--------|
| 准确性评估框架 | evaluator/accuracy_evaluator.py |
| 测试集标注（20份） | tests/fixtures/annotated/ |
| 定期评估机制 | evaluator/periodic_evaluator.py |
| 规则质量指标 | evaluator/rule_metrics.py |

**Phase 3 验收标准**：
- ✅ Precision ≥ 85%
- ✅ Recall ≥ 90%
- ✅ 高风险场景 Recall ≥ 95%

---

## 四、目录结构

```
agent_recheck/
├── pyproject.toml              # 项目配置
├── README.md                   # 项目说明
├── CHANGELOG.md               # 变更记录
│
├── cli/                        # 命令行入口
│   ├── __init__.py
│   ├── main.py                # 主入口
│   ├── commands/               # 子命令
│   │   ├── __init__.py
│   │   ├── analyze.py         # analyze 命令
│   │   ├── batch.py           # batch 命令
│   │   ├── rules.py           # rules 命令
│   │   ├── evaluate.py        # evaluate 命令
│   │   └── stats.py           # stats 命令
│   └── options.py             # 全局选项
│
├── analyzer/                   # 核心分析引擎
│   ├── __init__.py
│   ├── parser/                # 文档解析 ⭐
│   │   ├── __init__.py
│   │   ├── base.py           # 基类
│   │   ├── docx_parser.py    # DOCX 解析
│   │   ├── pdf_parser.py     # PDF 解析
│   │   ├── table_parser.py   # 表格解析 ⭐
│   │   └── shenzhen_adapter.py # 深圳格式适配 ⭐
│   │
│   ├── engine/                # 规则引擎
│   │   ├── __init__.py
│   │   ├── hybrid_engine.py   # 混合分析引擎 ⭐
│   │   ├── rule_loader.py    # 规则加载
│   │   ├── matcher.py        # 规则匹配
│   │   └── rule_manager.py   # 规则管理
│   │
│   ├── llm/                   # LLM 集成 ⭐
│   │   ├── __init__.py
│   │   ├── client.py         # LLM 客户端
│   │   ├── prompts.py        # Prompt 模板
│   │   ├── fallback.py       # 容错机制
│   │   ├── chunk_processor.py # 分段处理
│   │   └── consistency_checker.py # 一致性检查
│   │
│   ├── aggregator/            # 结果聚合
│   │   ├── __init__.py
│   │   ├── merger.py         # 结果合并
│   │   └── deduplicator.py  # 去重
│   │
│   └── consistency/           # 一致性检查
│       ├── __init__.py
│       ├── paragraph_checker.py
│       ├── standard_checker.py
│       └── regulation_checker.py
│
├── report/                     # 报告生成
│   ├── __init__.py
│   ├── base.py               # 基类
│   ├── json_reporter.py      # JSON 报告
│   ├── md_reporter.py        # Markdown 报告
│   └── html_reporter.py      # HTML 报告
│
├── knowledge/                  # 离线知识库
│   ├── __init__.py
│   ├── regulations/          # 法规原文
│   │   ├── national/         # 国家法规
│   │   │   ├── 政府采购法.md
│   │   │   ├── 政府采购法实施条例.md
│   │   │   ├── 87号令.md
│   │   │   └── 需求管理办法.md
│   │   └── shenzhen/         # 深圳地方法规 ⭐
│   │       ├── 深圳经济特区政府采购条例.md
│   │       └── 实施细则.md
│   ├── certifications/       # 认证证书规则
│   │   ├── iso9001.yaml
│   │   ├── iso14001.yaml
│   │   └── sa8000.yaml
│   └── sync/                  # 同步工具
│       └── sync.py
│
├── rules/                      # 规则配置
│   ├── _template.yaml         # 规则模板
│   ├── discrimination/        # 非歧视性规则
│   │   ├── DIS-001.yaml
│   │   └── ...
│   ├── procurement/          # 采购需求规则
│   ├── scoring/              # 评分标准规则
│   ├── contract/             # 合同条款规则
│   └── certifications/       # 认证规则
│
├── models/                     # 数据模型
│   ├── __init__.py
│   ├── document.py          # 文档模型
│   ├── rule.py              # 规则模型
│   ├── issue.py             # 问题模型
│   └── report.py            # 报告模型
│
├── utils/                      # 工具函数
│   ├── __init__.py
│   ├── logging.py            # 日志配置
│   ├── security.py           # 安全工具
│   └── path.py              # 路径工具
│
├── tracker/                    # 监控埋点
│   ├── __init__.py
│   ├── metrics.py           # 指标追踪
│   └── token_tracker.py     # Token 追踪
│
├── evaluator/                  # 准确性评估
│   ├── __init__.py
│   ├── accuracy_evaluator.py
│   ├── periodic_evaluator.py
│   └── rule_metrics.py
│
├── tests/                      # 测试套件
│   ├── __init__.py
│   ├── fixtures/             # 测试数据
│   │   ├── annotated/        # 已标注样本
│   │   └── synthetic/       # 合成样本
│   ├── test_parser.py
│   ├── test_engine.py
│   ├── test_llm.py
│   └── test_integration.py
│
└── config/                    # 配置文件
    ├── default.yaml          # 默认配置
    └── alerts.yaml           # 告警配置
```

---

## 五、依赖清单

```toml
[project]
name = "agent_recheck"
version = "0.1.0"
description = "政府采购招投标文件合规性审查智能体"
requires-python = ">=3.10"

dependencies = [
    # 文档解析
    "python-docx>=1.0.0",      # DOCX 解析
    "pdfplumber>=0.10.0",       # PDF 解析
    "PyMuPDF>=1.23.0",           # PDF 备选
    
    # LLM
    "openai>=1.0.0",            # LLM 调用
    
    # CLI
    "typer>=0.9.0",              # CLI 框架
    "rich>=13.0.0",             # 终端美化
    
    # 配置与数据
    "pyyaml>=6.0",              # YAML 解析
    "pydantic>=2.0.0",          # 数据验证
    
    # 日志与监控
    "structlog>=23.0.0",         # 结构化日志
    
    # 网络
    "httpx>=0.25.0",            # HTTP 客户端
    
    # 其他
    "tqdm>=4.65.0",             # 进度条
]

[project.optional-dependencies]
security = [
    "cryptography>=41.0.0",     # 配置加密
]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21.0",
    "ruff>=0.1.0",
    "mypy>=1.0",
]
```

---

## 六、CLI 命令设计

```bash
# 分析单个文件
agent_recheck analyze <file> [options]

# 批量分析
agent_recheck batch <directory> [options]

# 规则管理
agent_recheck rules list
agent_recheck rules add <rule.yaml>
agent_recheck rules validate
agent_recheck rules export

# 评估
agent_recheck evaluate [--test-set <path>]
agent_recheck evaluate --report

# 知识库
agent_recheck knowledge sync
agent_recheck knowledge status

# 监控
agent_recheck stats
agent_recheck stats --metrics <name>
```

---

## 七、验收标准汇总

| Phase | 周期 | 验收标准 |
|-------|------|---------|
| **Phase 0** | 2周 | 表格解析准确率 ≥ 80%，支持深圳格式 |
| **Phase 1** | 4周 | 12个核心规则 + LLM接入 + CLI可用 |
| **Phase 2** | 3周 | 规则总数 ≥ 40，一致性检查完成 |
| **Phase 3** | 2-3周 | Precision ≥ 85%, Recall ≥ 90% |

---

## 八、团队分工

| 角色 | 任务 |
|------|------|
| **Agent-1**: 项目脚手架 | Phase 0 全部 + Phase 1 基础架构 |
| **Agent-2**: 规则引擎 | Phase 1 规则 + Phase 2 规则扩展 |
| **Agent-3**: LLM 集成 | Phase 1 LLM + Phase 2 一致性 |
| **Agent-4**: 测试验证 | Phase 1-3 测试 + Phase 3 评估 |

---

## 九、开始开发

### Step 1: 创建项目基础结构
### Step 2: 实现文档解析模块（Phase 0）
### Step 3: 实现规则引擎（Phase 1 Week 3）
### Step 4: 实现 LLM 集成（Phase 1 Week 4）
### Step 5: 实现 CLI 和报告（Phase 1 Week 5-6）
### Step 6: 扩展规则和一致性（Phase 2）
### Step 7: 准确率评估（Phase 3）

---

**下一步行动**：开始 Step 1 - 创建项目基础结构
